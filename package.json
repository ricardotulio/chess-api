{
  "homepage": "https://github.com/ricardotulio/chess-api",
  "repository": "github:ricardotulio/chess-api",
  "author": {
    "name": "Ricardo Ledo de Tulio",
    "email": "ledo.tulio@gmail.com"
  },
  "main": "src/app.js",
  "dependencies": {
    "express": "^4.14.1"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-polyfill": "^6.22.0",
    "babel-preset-env": "^1.1.8",
    "babel-preset-es2015": "^6.22.0",
    "babel-register": "^6.22.0",
    "chai": "^3.5.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0"
  },
  "scripts": {
    "build": "babel src --out-dir build",
    "watch": "babel src --watch --out-dir build",
    "test": "mocha --recursive --require babel-polyfill --compilers js:babel-register",
    "coverage": "istanbul cover _mocha --root -- -R spec --recursive 'test/**/test.js'"
  }
}
